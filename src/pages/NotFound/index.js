import { ContainerNotFound } from './style';

export default function NotFound(){
    return(
        <ContainerNotFound>
            <span>Page not found.</span>
        </ContainerNotFound>
    );
}